formatter: "markdown table"

version: ">=0.16"

header-from: main.tf

sections:
  hide: []
  show:
    - header
    - requirements
    - providers
    - modules
    - resources
    - inputs
    - outputs

content: |-
  {{ .Header }}

  ## Usage

  Basic usage of this module is as follows:

  ```hcl
  module "example" {
  	source = "<module-path>"

  	# Required variables
  	{{ range .Module.Inputs }}{{ if .Required -}}
  	{{ .Name }} = 
  	{{ end }}{{ end }}
  }
  ```

  {{ .Requirements }}

  {{ .Providers }}

  {{ if .Module.ModuleCalls -}}
  ## Modules

  {{ range .Module.ModuleCalls }}
  - {{ .Name }} - {{ .Source }}{{ if .Version }} ({{ .Version }}){{ end }}
  {{- end }}
  {{ end }}

  {{ if .Resources -}}
  ## Resources

  The following resources are created:

  {{ range .Module.Resources }}
  - {{ .GetMode }}.{{ .Spec }} ({{ .Position.Filename }}#L{{ .Position.Line }})
  {{- end }}
  {{ end }}

  {{ .Inputs }}

  {{ .Outputs }}

output:
  file: "README.md"
  mode: inject
  template: |-
    <!-- BEGIN_TF_DOCS -->
    {{ .Content }}
    <!-- END_TF_DOCS -->

output-values:
  enabled: false
  from: ""

sort:
  enabled: true
  by: required

settings:
  anchor: true
  color: true
  default: true
  description: true
  escape: true
  hide-empty: false
  html: true
  indent: 2
  lockfile: true
  read-comments: true
  required: true
  sensitive: true
  type: true
